class Node {
  _inner;
  _marked;

  constructor() {}
}

export default class Graph {
  _map;

  constructor(map) {
    this._map = map;
  }

  DFS() {
    const recur = (node) => {
      if (node._ma) {
      }
    };
  }
}
