interface Member<T> {
  _root: T | null;
}

export class Tree<T> implements Member<T> {
  _root: T | null;

  constructor() {
    this._root = null;
  }

  BFS() {
    if (this.root === null) return;

    const 
  }

  DFS() {

  }
}
