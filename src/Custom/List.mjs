export default class List {
  constructor(_arr) {
    this._arr = _arr;
  }

  size() {
    return this._arr.length;
  }

  getDir

  getDir(_prop) {
    const dir = new Map();

    const listLen = this.size();
    for (let i = 0; i < listLen; ++i) {
      dir.set()
    }

    return dir;
  }
}
